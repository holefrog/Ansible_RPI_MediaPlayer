---
- name: Install base hardware and audio utilities
  become: yes
  ansible.builtin.apt:
    name:
      - i2c-tools
      - alsa-utils
      - libasound2t64  # Or libasound2 based on OS version
    state: present
    update_cache: yes